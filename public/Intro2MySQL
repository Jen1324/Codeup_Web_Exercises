
CREATE TABLE quotes(
	id UNSIGNED INT NOT NULL AUTO_INCREMENT,
	content VARCHAR(248) NOT NULL,
	author VARCHAR(50) NOT NULL,
	PRIMARY KEY (id)
);

-- -- WITH TEXT INSTEAD OF VARCHAR

-- CREATE TABLE quotes(
-- 	id UNSIGNED INT NOT NULL AUTO_INCREMENT,
-- 	content TEXT(248) NOT NULL,
-- 	author VARCHAR(50) NOT NULL DEFAULT 'Some Guy',
-- 	salary DECIMAL(8,2),
-- 	phone_number CHAR(10),
-- 	zip_code CHAR(5),
-- 	is_codeup_awesome ENUM('yes', 'duh')
-- 	PRIMARY KEY (id)
-- );

-- EXERCISE --
-- CREATE TABLE albums (
-- 	id INT NOT NULL AUTO_INCREMENT,
-- 	name VARCHAR(25) NOT NULL,
-- 	year DATE NOT NULL,
-- 	artist VARCHAR(50) NOT NULL,
-- 	PRIMARY KEY (id)
-- );

-- BASIC STATEMENTS: INSERT, ESCAPE --

-- INSERT INTO quotes VALUES (1, 'No Mr. Bond, I expect you to die!', 'Goldfinger'); --

--AUTO FILLS WHEN NULL.--
-- INSERT INTO quotes VALUES (NULL, 'Be excellent to each other.', 'Ted Theodore Logan'); --

-- INSERT INTO quotes (author, content)
-- VALUES ('Kurt Vonnegut', 'Those who believe in telekinetics, raise my hand.')


-- Terminal
-- SELECT * FROM  quotes id = 3
-- SELECT author, quotes
-- FROM quotes;
-- WHERE id BETWEEN 3 AND 5;

-- UPDATING VALUES

-- UPDATE quotes SET content = 'No, Mr. Bond, I expect you to die!'
-- WHERE id = 1;

-- DELETE FROM

-- SELECT * FROM quotes WHERE id = 4;
-- DELETE FROM quotes WHERE id = 4;

-- SELECT * FROM albums WHERE year < 1990;










